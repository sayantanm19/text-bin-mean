import { TestBed } from '@angular/core/testing';

import { PasteDetailsService } from './paste-details.service';

describe('PasteDetailsService', () => {
  let service: PasteDetailsService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(PasteDetailsService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
